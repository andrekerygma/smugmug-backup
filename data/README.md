# Local Data

This folder stores runtime data only, such as OAuth credentials, the account catalog, and album cache files.

Do not share these files. If you send the project folder to someone else, keep only this `README.md` and the placeholder in `data/albums/`.
